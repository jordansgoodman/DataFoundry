global:
  resolve_timeout: 5m

route:
  receiver: "null"
  group_by: ["alertname", "job"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: "null"

# To enable Slack, replace the receiver above with the slack config below:
#  - name: "slack"
#    slack_configs:
#      - api_url: "https://hooks.slack.com/services/REPLACE/ME"
#        channel: "#datafoundry-alerts"
#        send_resolved: true
#        title: "[{{ .Status | toUpper }}] {{ .CommonLabels.alertname }}"
#        text: "{{ range .Alerts }}{{ .Annotations.summary }}\n{{ .Annotations.description }}\n{{ end }}"
